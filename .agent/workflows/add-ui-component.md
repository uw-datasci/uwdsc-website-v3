---
description: Add a new shadcn UI component
---

This workflow adds a new shadcn/ui component to the shared `@uwdsc/ui` package and exports it.

// turbo

1. Add the component using the workspace root script:

```bash
pnpm ui:add <component-name>
```
