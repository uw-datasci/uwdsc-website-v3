/**
 * Placeholder shown inside an application answer input when the question does
 * not define its own. Applied when a question is created; admins can override
 * it per question.
 */
export const DEFAULT_QUESTION_PLACEHOLDER = "Type your answer here…";
